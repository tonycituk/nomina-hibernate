<persistence xmlns="http://xmlns.jcp.org/xml/ns/persistence" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence
             http://xmlns.jcp.org/xml/ns/persistence/persistence_2_1.xsd" version="2.1">

  <persistence-unit name="defaultPersistenceUnit">
    <description>
      Hibernate persistence configuration
    </description>

    <!-- JPA provider -->
    <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>

    <properties>
      <!-- JPA configuration -->
      <!-- ******************** -->

      <!-- Database driver. -->
      <property name="javax.persistence.jdbc.driver" value="org.mariadb.jdbc.Driver" />

      <!-- Relational database management system (RDBMS) connection URL. -->
      <property name="javax.persistence.jdbc.url" value="jdbc:mysql://localhost:3306/nomina_poo" />

      <!-- Database credentials. -->
      <property name="javax.persistence.jdbc.user" value="user_nomina" />
      <property name="javax.persistence.jdbc.password" value="abcd" />


      <!-- Hibernate configuration -->
      <!-- *********************** -->

      <!-- Show SQL generated by Hibernate. -->
      <property name="hibernate.show_sql" value="true" />

      <!--
      DDL stands for "Data Definition Language", learn more on:
      https://www.w3schools.in/mysql/ddl-dml-dcl/#DDL

      Long-story-short, DDL covers SQL statements such as: CREATE, ALTER and DROP.

      This configuration tells Hibernate what to do about DDL operations. Common
      values are the following:
      - "create": Before each execution, drop schema (and data) and create a new schema.
      - "update": Update existing schema, don't drop data.
      - "none": Do nothing with the schema.

      See: https://stackoverflow.com/a/1689769/12591546
      -->
      <property name="hibernate.hbm2ddl.auto" value="none" />

      <!-- Indicate SQL variant used by Hibernate. -->
      <property name="hibernate.dialect" value="org.hibernate.dialect.MySQL5Dialect" />
    </properties>
  </persistence-unit>
</persistence>